## What is Service?
- it is used to store a data and share logic so many components can use 
- instead of write same code in many component we can write it in one service

command
-       ng g s student-service