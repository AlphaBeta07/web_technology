# SQL
- *sql* => structural qurey language
- is used to communication beteween real-time databaase
- MySQL, PostgreSQL, SQLServer
- helps to add, update, delete
- realtime example -  bank, hospital etc

## Activity
create table   

    create table table-name(PRN INT(5), name char(10))

insert data  

    insert values into table-name(005, 'Anish')

select data

    select * from table-name

update

    update values into table-name

delete
